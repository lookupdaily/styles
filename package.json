{
  "name": "@lookupdaily/styles",
  "version": "1.0.0",
  "description": "My style library and utilities",
  "main": "dist/index.css",
  "files":[
    "src/**/*",
    "dist",
    "package.json",
    "README.md"
  ],
  "scripts": {
    "build": "sass src/index.scss dist/index.css --style=compressed",
    "lint": "stylelint 'src/**/*.scss' && npx prettier . --check",
    "lint:fix": "npx stylelint 'src/**/*.scss' --fix && npx prettier . --write"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prettier": "^3.3.3",
    "sass": "^1.77.8",
    "stylelint": "^16.8.1",
    "stylelint-config-standard-scss": "^13.1.0"
  }
}
